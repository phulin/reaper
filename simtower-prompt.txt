We're trying to create a behavior-identical clean-room design copy of 1993 Windows 3.1 game SimTower; we don't need the original UI but we do want a simulation with perfect fidelity. We're recording our findings in SPEC.md and in a ghidra database at analysis-2825a3c53f. First, review @SPEC.md and find anything unclear or too tied to binary implementation details. Add any items needed for a perfect-fidelity simulation to the blocker/missing details list at the end of the spec. Then review @SPEC.md list of blockers/implementation issues and resolve as many as you can by gathering information from the binary. Ghidra database for SIMTOWER.EX_ is in analysis-2825a3c53f. Investigate the ghidra decompiler output as a first pass, but you may have to look at the disassembly output too as sometimes the decompiler misses jump tables (jump tables are typically recorded at the end of a function with an array of values and a following array of corresponding near pointers for jump targets). record conclusions back to SPEC.md and ghidra as you go. SPEC should be a clean-room design when we are done: we should be able to implement something that plays identically to the original. Use `uv run` to run Python scripts. Output <promise>SPEC COMPLETE</promise> when you have high confidence that the spec could be used for a clean-room perfectly-faithful reimplementation.